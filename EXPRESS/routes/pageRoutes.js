const express=require("express");
const pageController=require("../controllers/pageController");

const router=express.Router();

router.get("/",);
router.get("/:id",);
router.get("/create",);
router.post("/create",)
router.delete("/:id",);

//second argument will be pageController

module.exports=router;